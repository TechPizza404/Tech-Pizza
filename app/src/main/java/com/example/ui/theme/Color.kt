package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// TechX Signature Color Tokens
val TechXBlue = Color(0xFF2563EB)       // High Density Vibrant Blue-600
val TechXBlueDark = Color(0xFF3B82F6)   // Tech Blue-500 for Dark Mode
val DeepSlate = Color(0xFF0F172A)       // Slate-900 text / accents
val SteelGray = Color(0xFF64748B)       // Slate-500 neutral text

// High Density Palette Elements
val HighDensityBg = Color(0xFFF7F9FB)    // Light slate background
val HighDensitySurface = Color(0xFFFFFFFF) // Flat white surface cards
val HighDensityBorder = Color(0xFFE2E8F0)  // Thin Slate-200 outline border
val HighDensityHeaderBg = Color(0xFFF8FAFC)// Slate-50 structured grid headers
val HighDensityVerdictBg = Color(0xFF1E3A8A)// Navy Blue-900 feedback block

// Dark Theme Colors
val DarkBackground = Color(0xFF0B0F19)  // Dark luxury slate background
val DarkSurface = Color(0xFF1E293B)     // Slate-800
val DarkOnSurface = Color(0xFFF1F5F9)   // Slate-100 ice text
val DarkBorder = Color(0xFF334155)      // Slate-700 border

// Standard Semantics
val MetricPositive = Color(0xFF10B981)  // Emerald-500 standard positive
val MetricPositiveBg = Color(0xFFD1FAE5)// Emerald-100 badge bg
val MetricNegative = Color(0xFFEF4444)  // Rose-500 unbiased con metric
val WarningYellow = Color(0xFFEAB308)   // Yellow-500 caution badge
val NeutralMuted = Color(0xFF94A3B8)    // Slate-400 description text

